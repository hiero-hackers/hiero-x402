/**
 * Kit parity with the sibling repos (payment-requests, checkout).
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  printWidth: 100,
  endOfLine: "lf",
  trailingComma: "all",
  arrowParens: "always",
};

export default config;
